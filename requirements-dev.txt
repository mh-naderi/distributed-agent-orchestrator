# Test-only dependencies. Not installed into the agent images - those stay
# minimal, see each agent's own requirements.txt.
-r orchestrator/requirements.txt

# The agent-side libraries, needed because the tests import the retrieval
# agent's store and exercise the research agent's search service.
sqlite-vec>=0.1.9
ddgs>=9.0
pyflakes>=3.2  # the code-analysis agent's static checks

pytest>=8.0
pytest-asyncio>=0.24
