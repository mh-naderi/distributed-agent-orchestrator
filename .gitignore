__pycache__/
*.pyc
.venv/
venv/
.env
*.log
.pytest_cache/

# Local vector index. In Kubernetes this lives on a PersistentVolume; locally
# it's a working file that shouldn't be committed.
data/

# Evaluation output. Regenerated by `python -m eval.run_eval` on every run,
# so tracking it means a large diff each time. The published results table in
# README.md is the durable record.
eval/results.json
